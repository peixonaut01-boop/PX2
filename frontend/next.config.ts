import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Nenhum cabeçalho customizado por enquanto – usamos as configs padrão do Next
};

export default nextConfig;
